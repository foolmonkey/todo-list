import React from "react";

const Add = () => {
  return (
    <div className="box">
      <button id="create">Add a task</button>
      <button>Options</button>
    </div>
  );
};

export default Add;
